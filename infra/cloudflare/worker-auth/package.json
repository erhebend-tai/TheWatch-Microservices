{
  "name": "thewatch-edge-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for edge JWT authentication validation — TheWatch API gateway",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
