@* Reusable status badge *@
<span class="status-badge @StatusClass">@Text</span>

@code {
    [Parameter] public string Text { get; set; } = "";
    [Parameter] public string StatusClass { get; set; } = "status-offline";
}
